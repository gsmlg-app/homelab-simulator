name: app_lib_core
description: Core utilities, IDs, enums, and helpers for homelab_simulator
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  equatable: ^2.0.7
  uuid: ^4.5.1

dev_dependencies:
  test: ^1.25.0
